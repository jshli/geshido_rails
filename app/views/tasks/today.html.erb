<%=react_component('Dashboard', {user: "#{@current_user.to_json}", projects: "#{@projects.to_json}", tasks: "#{@tasks.to_json}", currentMode: "tasks", greeting: "Here are your tasks for today", subheading: "Let's get productive"})%>
