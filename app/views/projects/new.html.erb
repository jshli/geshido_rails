<%=react_component('Dashboard', {user: "#{@current_user.to_json}", projects: "#{@projects.to_json}", tasks: "#{@tasks.to_json}", currentMode: "create project", greeting: "Create a new project", subheading: "What are we creating today?"})%>
